// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Your GitHub URL will be: https://<username>.github.io/<repo-name>
  site: 'https://beranekleon.github.io',
  base: '/beranekleon.github.io', 
});
